[plugin]
summary = "Configuration parameters"
version = "0.0.2"
description = "Dynamically configure system parameters and frontend display data"
author = "wu-clan"
tags = ["other"]
database = ["mysql", "postgresql"]

[app]
extend = "admin"

[api.config]
prefix = "/configs"
tags = "System configuration parameters"
